this project is a mini browser written using html, css,js and electron. To start, you need to copy the repository and write:npm init,npm i -g electron ,npm start  in terminal at the root of the project 
